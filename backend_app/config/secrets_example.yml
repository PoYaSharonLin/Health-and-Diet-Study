---
# Backend secrets configuration
# Copy this file to secrets.yml and fill in your values

development:
  DATABASE_URL: sqlite://backend_app/db/store/development.db
  AWS_ACCESS_KEY_ID: IAM_ACCESS_KEY
  AWS_SECRET_ACCESS_KEY: IAM_SECRET_KEY
  AWS_REGION: ap-northeast-1
  S3_BUCKET_NAME: S3_BUCKET_NAME
  # Condition assignment ticket queue; leave unset to fall back to least-count assignment
  REDIS_URL: redis://127.0.0.1:6379/0
  ASSIGNMENT_DEADLINE_SECONDS: '7200'

test:
  DATABASE_URL: sqlite://backend_app/db/store/test.db
  AWS_ACCESS_KEY_ID: IAM_ACCESS_KEY
  AWS_SECRET_ACCESS_KEY: IAM_SECRET_KEY
  AWS_REGION: ap-northeast-1
  S3_BUCKET_NAME: S3_BUCKET_NAME
  # No REDIS_URL in test: specs inject mock_redis or exercise the DB fallback

production:
  DATABASE_URL: postgres://user:password@host:port/database
  AWS_ACCESS_KEY_ID: IAM_ACCESS_KEY
  AWS_SECRET_ACCESS_KEY: IAM_SECRET_KEY
  AWS_REGION: ap-northeast-1
  S3_BUCKET_NAME: S3_BUCKET_NAME
  # On Render: use the Key Value instance's *internal* URL (same region as the web service)
  REDIS_URL: redis://red-xxxxxxxxxxxx:6379
  ASSIGNMENT_DEADLINE_SECONDS: '7200'
  # For TimescaleDB (optional upgrade):
  # DATABASE_URL: postgres://user:password@host:port/database
  # Then run: SELECT create_hypertable('behavior_events', 'timestamp');
